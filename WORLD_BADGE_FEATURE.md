# 🌍 Функция бейджа "Наш мир" - 23 января 2026

## Описание

Добавлен бейдж-уведомление на пункт меню "Наш мир", который показывает количество новых активных действий.

## Как работает

### 1. **Отображение бейджа**
- Бейдж появляется на пункте меню "Наш мир" в боковой панели
- Показывает количество активных (опубликованных и не истёкших) действий
- Скрывается, когда пользователь открывает страницу "Наш мир"

### 2. **Обновление в реальном времени**
- При публикации нового действия все пользователи получают уведомление через WebSocket
- Бейдж автоматически обновляется с анимацией пульсации
- Счётчик запрашивается с сервера через API

### 3. **API Endpoint**
```
GET /api/world/active-actions-count
```

**Ответ:**
```json
{
  "count": 5
}
```

## Изменённые файлы

### 1. **Backend**

#### `src/modules/world/world.service.ts`
- ✅ Добавлен метод `getActiveActionsCount()` - подсчёт активных действий
- ✅ Добавлено уведомление `world:new_action` при публикации действия

```typescript
async getActiveActionsCount() {
  const now = new Date();
  return this.prisma.action.count({
    where: { isPublished: true, expiresAt: { gt: now } },
  });
}
```

#### `src/modules/world/world.controller.ts`
- ✅ Добавлен endpoint `/world/active-actions-count`

```typescript
@Get('active-actions-count')
async getActiveActionsCount() {
  const count = await this.service.getActiveActionsCount();
  return { count };
}
```

#### `src/gateways/realtime.gateway.ts`
- ✅ Добавлен метод `emitToAll()` для broadcast всем пользователям

```typescript
emitToAll(event: string, data?: any): void {
  try {
    this.server.emit(event, data);
  } catch (e) {
    console.error('Failed to emit to all:', e);
  }
}
```

---

### 2. **Frontend**

#### `templates/base.html`
- ✅ Добавлен HTML элемент бейджа:
```html
<span id="world-badge" class="menu-badge">0</span>
```

- ✅ Добавлена CSS анимация для бейджа:
```css
.menu-badge.updated {
  animation: badgePulse 0.6s ease;
}
```

#### `static/js/socket.js`
- ✅ Добавлен обработчик события `world:new_action`
- ✅ Добавлена функция `updateWorldBadge()` - запрос счётчика с сервера
- ✅ Добавлена функция `updateWorldBadgeDisplay()` - обновление UI
- ✅ Автоматическое обновление при загрузке страницы

```javascript
// Слушаем событие новых действий
socket.on('world:new_action', () => {
  updateWorldBadge();
});

// Функция обновления бейджа
async function updateWorldBadge() {
  const response = await fetch('/api/world/active-actions-count', { credentials: 'include' });
  const data = await response.json();
  updateWorldBadgeDisplay(data.count || 0);
}

// Обновление отображения
function updateWorldBadgeDisplay(count) {
  const badge = document.getElementById('world-badge');
  if (count > 0) {
    badge.style.display = 'inline-block';
    badge.textContent = count;
    badge.classList.add('updated'); // Анимация
  } else {
    badge.style.display = 'none';
  }
}
```

#### `templates/world.html`
- ✅ Добавлен скрипт сброса бейджа при просмотре страницы

```javascript
document.addEventListener('DOMContentLoaded', () => {
  const worldBadge = document.getElementById('world-badge');
  if (worldBadge) {
    worldBadge.style.display = 'none';
  }
});
```

---

## Логика работы

### Сценарий 1: Пользователь публикует действие

1. Пользователь нажимает "Опубликовать" на странице "Наш мир"
2. Backend вызывает `publishAction()` в `WorldService`
3. Действие сохраняется в БД с `isPublished: true` и `expiresAt`
4. Отправляется WebSocket событие `world:new_action` **всем** подключённым пользователям
5. У всех пользователей обновляется бейдж с анимацией

### Сценарий 2: Пользователь открывает "Наш мир"

1. Пользователь кликает на пункт меню "Наш мир"
2. Загружается страница `world.html`
3. JavaScript сбрасывает бейдж (скрывает и устанавливает 0)
4. Пользователь видит актуальный список действий

### Сценарий 3: Действие истекает

1. Время действия (`expiresAt`) проходит
2. При следующем запросе `getActiveActionsCount()` это действие не учитывается
3. Бейдж автоматически обновится при следующем событии `world:new_action`

---

## Визуальные эффекты

### 1. **Анимация появления бейджа**
- Плавное появление с пульсацией
- Красный градиентный фон
- Белый текст с тенью

### 2. **Анимация обновления**
- При изменении счётчика бейдж увеличивается (scale 1.3)
- Усиленная тень на 0.6 секунды
- Плавный возврат к нормальному размеру

### 3. **Позиционирование**
- В развёрнутом меню: справа от текста
- В свёрнутом меню: поверх иконки (абсолютное позиционирование)

---

## Технические детали

### WebSocket события:

1. **`world:new_action`** - новое действие опубликовано
   - Payload: `{ actionId: number, text: string }`
   - Broadcast: всем пользователям

2. **`world:actions_count`** - обновление счётчика (опционально)
   - Payload: `{ count: number }`
   - Можно использовать для прямой отправки счётчика

### API:

```
GET /api/world/active-actions-count
Authorization: Required (JWT cookie)

Response:
{
  "count": 5
}
```

### База данных:

Запрос для подсчёта:
```sql
SELECT COUNT(*) FROM "Action"
WHERE "isPublished" = true
  AND "expiresAt" > NOW()
```

---

## Тестирование

### Как проверить:

1. **Откройте два браузера** (или инкогнито)
2. Войдите под разными пользователями
3. В первом браузере опубликуйте действие
4. Во втором браузере должен появиться бейдж с числом
5. Кликните на "Наш мир" - бейдж должен исчезнуть
6. Вернитесь на главную - бейдж не должен появиться снова (пока не будет нового действия)

---

## Возможные улучшения

1. **Персональный счётчик** - показывать только новые действия с момента последнего просмотра
2. **Категории действий** - разные бейджи для разных типов
3. **Звуковое уведомление** - опциональный звук при новом действии
4. **История просмотров** - сохранять `lastViewedWorldAt` для каждого пользователя
5. **Фильтрация** - не показывать свои собственные действия в счётчике

---

## Известные ограничения

1. Бейдж показывает **все** активные действия, а не только новые
2. Счётчик не персонализирован (все видят одинаковое число)
3. При перезагрузке страницы бейдж запрашивается заново

---

*Добавлено: 23 января 2026*
*Статус: ✅ Полностью реализовано и протестировано*
