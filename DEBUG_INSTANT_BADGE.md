# 🐛 Отладка мгновенных бейджей

## ✅ Что сделано

1. ✅ Код в `socket.js` изменен правильно (мгновенное обновление +1)
2. ✅ Код в `world.service.ts` правильный (отправка WebSocket события)
3. ✅ Добавлено версионирование файлов JS (`?v=3`)
4. ✅ Добавлены console.log для отладки

---

## 🔍 Проверка работоспособности

### Шаг 1: Перезапустите сервер
```bash
# Остановите текущий процесс (Ctrl+C)
npm run start

# Или в Docker
docker compose down
docker compose up --build
```

### Шаг 2: Очистите кеш браузера

#### Chrome/Edge:
1. `Ctrl + Shift + Delete`
2. Выберите "Изображения и файлы кэша"
3. Нажмите "Удалить данные"

**ИЛИ**

1. Откройте DevTools (`F12`)
2. Правый клик на кнопке обновления
3. "Очистить кэш и жесткая перезагрузка"

#### Firefox:
1. `Ctrl + Shift + Delete`
2. Выберите "Кэш"
3. Нажмите "Удалить сейчас"

---

### Шаг 3: Проверьте консоль браузера

#### Откройте DevTools (`F12`) → Console

**При загрузке страницы должны появиться:**
```
✅ [Socket] Connected to /world namespace, joining room user_123
```

**При публикации действия должны появиться:**
```
🔥 [WebSocket] world:new_action received: {actionId: 123, text: "...", createdAt: "..."}
⚡ [Badge] Updating from 0 to 1
```

---

### Шаг 4: Проверьте файл socket.js

#### Откройте DevTools → Sources → static/js/socket.js

**Проверьте, что есть эти строки (около строки 78-100):**
```javascript
socket.on('world:new_action', (data) => {
  console.log('🔥 [WebSocket] world:new_action received:', data);
  const isWorldPage = window.location.pathname.includes('/world/');
  
  if (!isWorldPage) {
    // 🚀 МГНОВЕННОЕ обновление бейджа (+1) без fetch запроса
    const badge = document.getElementById('world-badge');
    if (badge) {
      const currentCount = parseInt(badge.textContent) || 0;
      console.log('⚡ [Badge] Updating from', currentCount, 'to', currentCount + 1);
      updateWorldBadgeDisplay(currentCount + 1);
    }
  }
});
```

**Если этих строк нет** → браузер загрузил старую версию из кеша!

---

## 🧪 Тест мгновенного обновления

### Тест 1: Два браузера
1. ✅ Откройте главную страницу в **Chrome**
2. ✅ Откройте "Наш мир" в **Firefox** (или инкогнито)
3. ✅ Откройте консоль в Chrome (`F12`)
4. ✅ Опубликуйте действие в Firefox
5. ✅ В консоли Chrome должно появиться:
   ```
   🔥 [WebSocket] world:new_action received: {...}
   ⚡ [Badge] Updating from 0 to 1
   ```
6. ✅ Бейдж должен появиться **мгновенно** (без задержки)

---

### Тест 2: Проверка WebSocket подключения

**В консоли браузера выполните:**
```javascript
window.socket.connected
```

**Должно вернуть:** `true`

**Если `false`** → WebSocket не подключен!

---

## 🚨 Возможные проблемы

### Проблема 1: Старая версия файла
**Симптомы:** В консоли нет логов `🔥 [WebSocket]`

**Решение:**
1. Проверьте, что в `base.html` есть `?v=3`:
   ```html
   <script src="/static/js/socket.js?v=3"></script>
   ```
2. Жесткая перезагрузка: `Ctrl + F5`
3. Откройте в инкогнито режиме

---

### Проблема 2: WebSocket не подключен
**Симптомы:** В консоли нет `✅ [Socket] Connected`

**Проверка:**
```javascript
console.log(window.socket);
```

**Решение:**
1. Проверьте, что `CURRENT_USER_ID` установлен:
   ```javascript
   console.log(window.CURRENT_USER_ID);
   ```
2. Проверьте, что сервер запущен
3. Проверьте консоль сервера на ошибки

---

### Проблема 3: События не приходят
**Симптомы:** WebSocket подключен, но событие `world:new_action` не приходит

**Проверка на сервере:**
```typescript
// src/modules/world/world.service.ts (строка 72-81)
console.log('📢 [Server] Emitting world:new_action:', { actionId, text });
this.rt.emitToAll('world:new_action', { ... });
```

**Решение:**
1. Проверьте логи сервера
2. Проверьте, что `RealtimeGateway` инициализирован
3. Проверьте, что `emitToAll` работает

---

### Проблема 4: Бейдж не обновляется
**Симптомы:** События приходят, но бейдж не меняется

**Проверка:**
```javascript
// В консоли браузера
document.getElementById('world-badge');
```

**Должно вернуть:** `<span id="world-badge" ...>`

**Если `null`** → элемент бейджа не найден!

**Решение:**
1. Проверьте, что вы авторизованы
2. Проверьте, что в `base.html` есть `#world-badge`

---

## 📊 Ожидаемое поведение

### Пользователь A (на главной):
```
1. Страница загружена
   → ✅ [Socket] Connected
   
2. Пользователь B публикует действие
   → 🔥 [WebSocket] world:new_action received
   → ⚡ [Badge] Updating from 0 to 1
   → Бейдж появляется МГНОВЕННО ⚡
```

### Пользователь B (на "Наш мир"):
```
1. Страница загружена
   → ✅ [Socket] Connected
   
2. Публикует действие
   → 📢 [Server] Emitting world:new_action
   
3. Получает своё же событие
   → 🔥 [WebSocket] world:new_action received
   → 📄 [World Page] Refreshing published actions...
   → Список обновляется
```

---

## 📝 Чек-лист

- [ ] Сервер перезапущен
- [ ] Кеш браузера очищен
- [ ] `socket.js?v=3` в base.html
- [ ] В консоли есть `✅ [Socket] Connected`
- [ ] В консоли есть `🔥 [WebSocket] world:new_action received`
- [ ] Бейдж появляется мгновенно
- [ ] Бейдж исчезает при открытии "Наш мир"

---

## 🆘 Если ничего не помогло

### Проверьте файл напрямую
Откройте в браузере:
```
http://localhost:3000/static/js/socket.js
```

Найдите строку (около 78-100):
```javascript
socket.on('world:new_action', (data) => {
  console.log('🔥 [WebSocket] world:new_action received:', data);
```

**Если этой строки нет** → файл не обновился на сервере!

**Решение:**
1. Проверьте, что вы сохранили файл
2. Перезапустите сервер
3. Проверьте права доступа к файлу

---

*Дата: 23 января 2026*  
*Версия socket.js: v3*
